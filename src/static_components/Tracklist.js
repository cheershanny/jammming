export const Tracklist = (prop) => {
    return (
        <p className = {prop.className} >Hi! my name is Tracklist and I think I know everything about the song that is supposed to play now</p>
        );
}